<% if current_user == @pet.user %>
  <p><%= link_to 'よくある質問を投稿する', new_pet_question_path(pet), class: "btn-sm btn-primary" %></p>
<% end %>
      <h1 class="question_title">よくある質問</h1><p class="questions_count">(全<%= questions.count %>件)</p>
      <div class="questions">
        <% @pet.questions.each do |question| %>
              <%= link_to question.title,  pet_question_path(question.pet_id, question.id), class: "question"%>
        <% end %>
      </div>